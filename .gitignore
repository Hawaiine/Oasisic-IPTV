# 校验产物不进仓库
output/check_result.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/

# 采集原始缓存
data/all_channels.json
data/*.csv
!data/.gitkeep
!data/channels.json
!data/aliases.json
!data/source_stats.json

# 全量/临时 XML 不提交
output/*.xml

# 探活分列：本机探活产物（内容取决于出口网络），不进仓库
output/live_ipv6.m3u
output/live_cmcc.m3u
output/live_signed.m3u

# EPG 上游盘点产物（机器可读，随时可重生成）
output/epg_inventory.json

# 扩容候选清单（由 scripts/suggest_channels.py 重生成）
data/channels_candidates.json

# 系统
.DS_Store
Thumbs.db
*.swp
*.swo
*.bak
.env
.env.*
*.token
